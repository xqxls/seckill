<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.myseckill.mapper.GoodsMapper">

    <select id="findGoodsVo" resultType="com.example.myseckill.vo.GoodsVo">
        SELECT g.id,
               g.goods_name,
               g.goods_title,
               g.goods_img,
               g.goods_price,
               g.goods_stock,
               sg.seckill_price,
               sg.stock_count,
               sg.start_date,
               sg.end_date
        FROM goods g
                 LEFT JOIN seckill_goods sg on g.id = sg.goods_id
    </select>

    <select id="findGoodsVobyGoodsId" resultType="com.example.myseckill.vo.GoodsVo">
        SELECT g.id,
               g.goods_name,
               g.goods_title,
               g.goods_img,
               g.goods_price,
               g.goods_stock,
               sg.seckill_price,
               sg.stock_count,
               sg.start_date,
               sg.end_date
        FROM goods g
                 LEFT JOIN seckill_goods sg on g.id = sg.goods_id
        WHERE
            g.id=#{goodsId}
    </select>

</mapper>
